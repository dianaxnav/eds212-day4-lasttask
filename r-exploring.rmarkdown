---
title: "Exploring"
---

```{r}
library(tidyverse)
library(skimr)
library(GGally)
```

```{r}
names(diamonds)
summary(diamonds)
skim(diamonds)
```

```{r}
ggpairs(diamonds)
```

```{r}
ggplot(data = diamonds, aes(x = price, y = carat)) + geom_point(color = "pink")
```

```{r}
ggplot(data = diamonds, aes(x = price)) +
  geom_histogram(aes(fill = carat))
```

